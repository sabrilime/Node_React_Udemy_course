let express,readdirSync,cors,mongoose;_427‍.w("express",[["default",["express"],function(v){express=v}]]);_427‍.w("fs",[["readdirSync",["readdirSync"],function(v){readdirSync=v}]]);_427‍.w("cors",[["default",["cors"],function(v){cors=v}]]);_427‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);



const morgan = require('morgan');
require('dotenv').config();

const app = express();

//DB Connection
mongoose
  .connect(process.env.DATABASE, {
    //useNewUrlParser: true, 
    //useUnifiedTopology: true
  })
  .then(() => console.log("DB connected"))
  .catch((err) => _427‍.g.console.log("DB Error => ", err));

//midllewares
app.use(cors());
app.use(morgan('dev'));
app.use(express.json());

// route middleware
readdirSync('./routes').map(
    (r) => app.use('/api', require(`./routes/${r}`))
);

const port = process.env.PORT || 8000;

app.listen(port, () => console.log(`Server is running on port ${port}`));