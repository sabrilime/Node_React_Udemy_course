let mongoose,bcrypt;_e4f‍.x([["default",()=>_e4f‍.o]]);_e4f‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_e4f‍.w("bcrypt",[["default",["bcrypt"],function(v){bcrypt=v}]]);


const {Schema} = mongoose

const userSchema = new Schema({
    name: {
        type: String,
        trim: true,
        required: 'Name is required'
    },
    email: {
        type: String,
        trim: true,
        required: 'Email is required',
        unique: true
    },
    password: {
        type: String,
        required: true,
        min: 6,
        max: 64
    },
    stripe_account_id: '',
    stripe_seller: {},
    stripeSession: {},
},{
    timestamps: true
});

// hash password only if user is changing the password or registering for the first time
userSchema.pre('save', function(next) {
    let user = this;
    if(user.isModified('password')) {
        return bcrypt.hash(user.password, 12, function(err, hash) {
            if(err) {
                _e4f‍.g.console.log('BCRYP HASH ERROR ', err);
                return next(err);
            }
            user.password = hash;
            return next();
        })
    }
    else {
        return next();
    }
});

userSchema.methods.comparePassword = function (password, next) {
    bcrypt.compare(password, this.password, function(err, match) {
        if(err) {
            _e4f‍.g.console.log('COMPARE PASSWORD ERROR', err);
            return next(err, false); 
        }
        _e4f‍.g.console.log("MATCH PASSWORD", match)
        return next(null, match);
    })
}

_e4f‍.d(mongoose.model('User', userSchema));